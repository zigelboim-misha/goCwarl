apiVersion: v1
kind: Service
metadata:
  name: {{ include "gocrawl.fullname" . }}
  labels:
    {{- include "gocrawl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gocrawl.selectorLabels" . | nindent 4 }}
